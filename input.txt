class Default {
  // this is the constructor
  init() {
    // it should be able to set<|SPACE|>    // properties on the instance
    this.x = "foo";
    this.y = 91;
  }
}

// the constructor should be called
// automatically  when the class is being<|SPACE|>// instantiated
print Default().x;
print Default().y;